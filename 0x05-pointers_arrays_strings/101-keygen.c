#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - main block
 * Descreption: genrates a random number for  101-crackme
 * Return: always return 0
 */
int main(void)
{
	srand(time(NULL));
	printf(%d, rand());
	return (0);
}
