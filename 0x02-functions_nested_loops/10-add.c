#include <stdio.h>
#include "main.h"
/**
 * add - adds twon number and prints the sum
 * @a: frist parameter
 * @b: second parameter
 *
 * Return: the sum of the two numbers
 */
int add(int a, int b)
{
	return (a + b);
}
