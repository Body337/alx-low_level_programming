#include <stdio.h>
/**
 * main - prints the file name
 * @argc: number of arguments passed to the function
 * @argv: a string containing the arguments
 *
 * Return: 0 on success
 */
int main(int __attribute__ ((unused)) argc, char **argv)
{
	printf("%s\n", argv[0]);

	return (0);
}
